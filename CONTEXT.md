# AutoBlade 领域词汇

本文固定 AutoBlade 在叶片建模领域使用的规范术语。历史发布材料可以保留当时的名称；当前设计和新增文档应优先使用下列词汇。

## 产品与执行

**AutoBlade**：
从翼型和展向截面定义生成三维叶片模型的命令行产品；CAD 厂商是可选择的建模后端，而不是产品身份。
_避免_：在描述 `0.3.0` 目标产品时使用 CATIA AutoBlade

**CAD 后端**：
一次建模调用中唯一选定、负责创建原生模型和中性模型的 CAD 能力提供者。
_避免_：CAD 插件、自动回退后端

**建模任务**：
一个已经闭合全部输入引用、输出位置和建模选项的单叶片工作单元。
_避免_：把一个批次或参数组合称为一个任务

**制品集**：
一次成功建模共同产生且必须作为整体校验、冲突处理和发布的原生模型与 STEP 模型。
_避免_：把单个成功文件视为完整结果

**认证运行时**：
已经通过指定真实 CAD、黄金基线、安装和进程清理门禁的平台与版本组合。
_避免_：把“可以尝试运行”称为“已支持”

## 输入与模型

**翼型**：
位于规范平面、按尾缘到前缘再回到尾缘排序的归一化点序列，用作一个或多个桨叶截面的基准轮廓。
_避免_：截面、叶片

**桨叶截面**：
一个翼型在指定展向位置经过弦长缩放、扭转和平移后得到的实际轮廓。
_避免_：翼型文件、截面参数集合

**桨叶定义**：
按展向顺序排列、并已明确引用翼型的全部桨叶截面记录。
_避免_：把六列模板在尚未绑定翼型时称为完整桨叶定义

**尖尾缘**：
翼型首尾点精确重合、上下表面在同一尾缘顶点闭合的拓扑。
_避免_：用未声明的容差把近似重合点视为尖尾缘

**钝尾缘**：
翼型首尾点不重合、需要显式尾缘闭合边连接上下表面的拓扑。
_避免_：开口尾缘

## 几何与验证

**原生模型**：
由所选 CAD 后端保存、保留该 CAD 可理解对象和依赖关系的模型文件；CATIA 使用 CATPart，FreeCAD 使用 FCStd。
_避免_：把 STEP 称为原生模型

**中性模型**：
由最终有效实体导出的 STEP 几何，用于跨 CAD 交换和几何比较。
_避免_：把 STEP 的面号或边号当作跨后端稳定身份

**参考几何**：
为检查初始叶片走向而保存、但不作为最终实体权威输入的前缘或后缘曲线快照。
_避免_：导引约束、可编辑驱动

**可重算依赖**：
原生模型中截面到最终实体的 CAD 原生依赖关系；AutoBlade 保证生成模型保存重开后可无修改重算，不保证用户编辑后的几何仍然有效。
_避免_：完整参数化输入、任意编辑保证

**几何等价**：
通过实体属性、截面、表面、体积、质心和包围盒的标准测量报告描述两个后端的
差异，实际可用性由人类按用途判断；不要求文件字节、面数、边数或拓扑编号相同。
_避免_：二进制相等、拓扑编号相等

**黄金基线**：
经许可审查、版本化并由工程责任人批准的输入、CATIA STEP、来源信息、摘要和
固定测量方法集合。它提供稳定参照，不自动裁决模型可用性；见
[人工判断规则](docs/adr/0007-measure-geometry-and-defer-usability-to-humans.md)。
_避免_：开发者临时输出、由 FreeCAD 失败测试自动改写的期望结果
